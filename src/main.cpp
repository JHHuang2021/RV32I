#include <iostream>
#include <array>
#include "include/tomasulo.h"
Tomasulo CPU;
int main() {
    //magic
    // freopen("testcases/magic.data", "r", stdin);
    // freopen("test.out", "w", stdout);
    CPU.Run();
    return 0;
}