#include <bits/stdc++.h>

#include "include/tomasulo.h"
Tomasulo CPU;

int main() {
    freopen("testcases/tak.data", "r", stdin);
    freopen("test.out", "w", stdout);
    CPU.Run();
    return 0;
}